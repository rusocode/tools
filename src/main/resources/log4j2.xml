<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE xml>

<!-- Configura el logger utilizando el nivel de eventos internos en "WARN" para registrar los mensajes 
	en consola. -->
<Configuration status="WARN" strict="true">

	<Properties>
		<Property name="LOG_DIR">target/logs</Property>
	</Properties>

	<Appenders>

		<!-- Salida en consola -->
		<Console name="Console" target="SYSTEM_OUT">
			<PatternLayout pattern="[%d{HH:mm:ss}] [%c/%p]: %m%n" />
		</Console>

		<!-- Salida en archivo con renovacion. "Vimos una mejora del rendimiento del 20-200% en comparación 
			con RollingFileAppender con "bufferedIO=true" en nuestras mediciones." -->
		<RollingRandomAccessFile name="RollingRandomAccessFile"
			fileName="${LOG_DIR}/latest.log" filePattern="${LOG_DIR}/%d-%i.log.gz">
			<!-- Patron de conversion. En el especificador de conversion "%-5p", -5 es el modificador de formato 
				y p es el caracter de conversion. -->
			<PatternLayout pattern="[%d{HH:mm:ss}] [%t/%level]: %m%n" />
			<!-- [%d{HH:mm:ss}] [thread %c{1}/%level]: %m%n -->
			<!-- Politicas de renovacion -->
			<Policies>
				<!-- Solo tiene que ir una politica -->
				<!-- <TimeBasedTriggeringPolicy /> -->
				<!-- Tamaño limite del archivo -->
				<SizeBasedTriggeringPolicy size="10 KB" />
			</Policies>
		</RollingRandomAccessFile>

	</Appenders>

	<!-- Configura los loggers para cada appender -->
	<Loggers>
		<!-- Indica el nombre del paquete que ejecuta el logger en el atributo name y establece la aditividad 
			en falso para que el root (padre del appender de la consola) no repita los mensajes. -->
		<Logger name="log" level="debug" additivity="false">
			<AppenderRef ref="Console" />
		</Logger>
		<Logger name="log" level="debug">
			<AppenderRef ref="RollingRandomAccessFile" />
		</Logger>
		<!-- Nivel raiz -->
		<Root level="all">
			<AppenderRef ref="Console" />
		</Root>
	</Loggers>

</Configuration>
